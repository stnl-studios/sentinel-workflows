# Sentinel Workflows

When a Sentinel spec exists, follow its approved workflow and use English for workflow artifacts. Do not implement without a developer-approved `plan-execution.md` and `test-plan.md`. Respect each slice's allowed, read, and blocked paths. Validate with objective evidence, never a bare claim that tests passed. Do not bypass human gates, expand scope, or run a phase from free conversation.
