# Preserved C19 task artifact

The managed replay preserved the selected task artifact before cleanup. The operation's delegation blocker reported that the runner returned non-canonical mandatory field labels, including `Escopo verificado`, `Estado testado`, `Comandos executados`, and `Testes selecionados`, instead of the canonical execution-record schema fields.
