Use `stnl-slice-executor`.
OPERATION=APPLY_FINDINGS
SPEC_PATH={{SPEC_PATH}}
SLICE={{SLICE}}
MANAGED_WORKSPACE={{MANAGED_WORKSPACE}}
BENCHMARK_SEQUENCE={{BENCHMARK_SEQUENCE}}
Treat the concrete recovery operation and slice returned by the shared deterministic preflight as authority; derive neither from the current request, and report both exactly when blocked.
Leia `Requirements authority` exclusivamente do campo exato `authority=sha256:<64hex>` retornado pelo mesmo official preflight para este `SPEC_PATH`, operação e slice, exija igualdade com o artifact selecionado e copie o valor byte-identical para o payload do runner e candidate record. Nunca calcule hash raw de `shared/requirements.md` ou `feature_spec.md` nem reconstrua a authority; se preflight e artifact concordarem, um valor diferente criado pelo producer não é documentary divergence.
Para cada campo file-backed definido pelo execution-record contract como task-relative, derive o valor persistido do target físico final com `path.relative(dirname(tasks/slice-NN.md), target)` e normalize separadores para `/`. Use o artefato detalhado final como base em `Changed Areas`, `Corrections Applied`, `Correction paths` e ao persistir `Tested state`; nunca use CWD, workspace root, repository root, SPEC root ou candidate root, e não converta silenciosamente uma saída com base errada.
Inclua no payload do runner `RUNNER_EVIDENCE_SERIALIZER=__RUNNER_EVIDENCE_SERIALIZER__`. O launcher fornece os valores absolutos e canônicos `MANAGED_WORKSPACE={{MANAGED_WORKSPACE}}`, `SPEC_PATH={{SPEC_PATH}}` e `SLICE={{SLICE}}`; copie-os exatamente e não use placeholders. Depois de decidir semanticamente findings, checks e comandos, retorne somente um objeto JSON puro, sem markdown, preâmbulo ou pós-escrito, com exatamente as chaves semânticas lowerCamelCase `status`, `automaticCheckRound`, `findingsCycle`, `head`, `discoverySources`, `discoveryActions`, `verificationTypesConsidered`, `nonApplicabilityRationale`, `noVerificationCommandConfirmation`, `commands`, `resultOfEachCommandAndExitCode`, `selectedChecks`, `selectionRationale`, `coverage`, `findingsVerified`, `correctionsCovered`, `regressionsSelected`, `unsupportedActiveFindings`, `failures`, `evidenceOrFailureSummary`, `affectedFilesOrBehaviors`, `blockers`, `unexpectedWorkspaceEffects` e `persistenceSummary`; `commands` é um array de objetos com exatamente `command` e `exit`, e não contém labels ou tuples formatados. O contexto principal persiste esse JSON sem alteração em arquivo temporário externo e invoca exatamente `node "$RUNNER_EVIDENCE_SERIALIZER" --execution-bundle --operation APPLY_FINDINGS --workspace "{{MANAGED_WORKSPACE}}" --spec-path "{{SPEC_PATH}}" --slice "{{SLICE}}" --semantic-response-file <absolute-temp-file>`. A boundary determinística rejeita chaves ausentes, desconhecidas ou valores ambíguos, canonicaliza `Changed Areas` e `Corrections Applied` usando os targets físicos aprovados em `Checklist expected areas` e `path.relative(dirname(taskArtifact), target)` antes da candidate validation, aceita alias relativo ao workspace somente quando ele identifica unicamente um target aprovado e bloqueia claims ambíguos ou irresolvíveis; depois deriva os targets canônicos, verifica `realpath`, calcula `path.relative` com `/`, e serializa deterministicamente labels, ordem, delimiters, hashes, response e record. Se o JSON ou a serialização sair diferente de zero, retorne `Status: BLOCKED` no contexto principal; não repare depois da candidate validation nem publique saída inválida.
Após aplicar os findings e registrar correções e escopo alterado, prepare o payload semântico mínimo para o runner configurado e envie-o por stdin ao único launcher permitido: `node "__RUNNER_INVOCATION_HELPER__" --operation APPLY_FINDINGS --sequence {{BENCHMARK_SEQUENCE}} --slice "{{SLICE}}" <<'SENTINEL_RUNNER_REQUEST'`. Inclua somente `SPEC_PATH`, execution root derivado, slice, paths de plans e tasks, Requirements authority, Plan revision, o official execution validator/preflight aplicável, findings ativos, correções aplicáveis, escopo alterado, evidências compactas relevantes, rodada automática e contexto estritamente necessário; não envie histórico da conversa. O helper lê as instruções e perfil exatos de `.codex/agents/stnl_validation_runner.toml`, inicia uma nova sessão `codex exec` sem histórico, anexa ao argv real o schema fechado específico da operação, desabilita delegação multi-agent, persiste o JSONL bruto e extrai deterministicamente a mensagem final sem reescrevê-la. Feche o heredoc com `SENTINEL_RUNNER_REQUEST`. O recibo `SENTINEL_RUNNER_RECEIPT` retorna o `semanticResponseFile`; passe esse arquivo byte-for-byte ao producer de evidência. Invoque o runner no mínimo uma vez e no máximo três vezes nesta sequência oficial, nas rodadas `1/3`, `2/3` e `3/3`; nunca faça uma quarta chamada. Cada invocação conta como tentativa e não há fallback, subagent nativo ou comando `codex exec` montado pelo modelo. A descoberta independente e `TESTS_NOT_APPLICABLE` continuam sendo decisão do runner.
Não passe logs completos. Não execute no contexto principal testes, builds, linters, typechecks, compilações ou outros comandos de verificação. Aguarde cada retorno e persista-o append-only em `Findings Test Evidence`, com o próximo `findings-check-NN` global, ciclo de findings, rodada, descoberta de checks, justificativa de não aplicabilidade, estado, comandos, falhas, correções cobertas e escopo.
Em `TESTS_FAIL` nas rodadas 1 ou 2, depois de persistir a evidência, ajuste automaticamente no contexto principal somente findings persistidos, falhas introduzidas ou expostas pelas correções e regressões diretamente relacionadas dentro do escopo aprovado; registre falha, evidência, alteração, arquivos, correções e escopo atualizado, e então chame o runner novamente. Na terceira falha, persista, preserve os findings ativos, entre em `FINDINGS_RETRY_EXHAUSTED` e encerre sem nova correção automática; a única próxima ação da slice é `VALIDATE_SLICE`, e nenhuma operação do executor pode reentrar antes do veredito formal.
Aceite somente `TESTS_PASS`, `TESTS_FAIL`, `TESTS_NOT_APPLICABLE` ou `BLOCKED`. Encerre em `TESTS_PASS`, `TESTS_NOT_APPLICABLE`, `BLOCKED`, terceira falha ou decisão fora do escopo; preserve findings e Validation Attempts, mantenha a slice aberta e não crie Effective Validation Base ou conclusão `[x]`. `TESTS_NOT_APPLICABLE` exige descoberta objetiva, fontes e ações read-only resumidas, justificativa e nenhum comando de verificação executado e não resolve findings por si só.
Se a sessão não iniciar ou o transporte falhar, faça no máximo uma nova tentativa técnica em outra sessão independente com o mesmo payload mínimo. Essas tentativas não consomem rodada `N/3`, não criam `findings-check-NN`, não autorizam correção e, após a segunda falha, persistem o `Delegation Blocker` canônico de inicialização (compatibilidade: `Runner Initialization Blocker`); saída malformada usa o mesmo singleton com `Kind: malformed-output` e não recebe retry de transporte. Não faça fallback, não marque findings como resolvidos sem sustentação, não crie etapa manual de retry, não amplie escopo e não inicie `VALIDATE_SLICE` nem outra operação automaticamente.
Se a mesma operação retornar após correções e escopo já persistidos com bloqueio anterior exclusivamente de inicialização, retome diretamente no spawn: não reaplique findings, não duplique checklist, diff, evidência ou blocker, não reinicie identificadores e não crie rodada antes de o runner iniciar. Nunca crie scratch file, script auxiliar, manifest ou relatório ad hoc dentro da SPEC; reporte qualquer path não canônico exatamente e use somente diretório temporário externo quando necessário.

Para a resposta final semântica, não invoque serializer em modo `--response` nem escreva labels mecânicos. O contexto principal usa uma única chamada `node "$RUNNER_EVIDENCE_SERIALIZER" --execution-bundle --operation APPLY_FINDINGS --workspace "{{MANAGED_WORKSPACE}}" --spec-path "{{SPEC_PATH}}" --slice "{{SLICE}}" --semantic-response-file <absolute-temp-file>`; copie somente o bundle completo emitido pela boundary determinística para o artifact e candidate. Não faça reparo após rejeição.

Use o `semanticResponseFile` retornado pelo launcher; ele foi capturado do JSONL bruto pela função determinística `captureRunnerResponse`, sem desembrulhar, reserializar, traduzir ou reparar o objeto JSON.

Contexto adicional (opcional):
