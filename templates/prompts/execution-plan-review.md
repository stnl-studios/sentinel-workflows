Use `stnl-spec-execution-manager`.

SPEC: `{{SPEC_PATH}}`
Execution root: `{{EXECUTION_ROOT}}` (optional; infer it when blank).
Prefer this review in the same session as planning. Reread the plan and correct it directly for oversized or undersized phases, hidden dependencies, migrations, external dependencies, breaking changes, uncovered criteria, shared files or state, unsafe parallelism, and phases without observable results.
Do not implement code or create tasks. Save the corrected plan and do not reproduce it in chat.

Reply only with:
- adjustment count and types;
- blockers;
- confirmation that the plan was updated.
