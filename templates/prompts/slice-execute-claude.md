Use `stnl-slice-executor`.
OPERATION=EXECUTE_SLICE
SPEC_PATH={{SPEC_PATH}}
SLICE={{SLICE}}

Treat the concrete recovery operation and slice returned by deterministic preflight as authority; derive neither from this request. On `RUNNER_RESULT_BLOCKED`, resume the stored operation/slice without reimplementation, checklist reset, history rewrite or new round allocation.

After the owner implements and records approved scope, delegue obrigatoriamente to `@agent-stnl-validation-runner` in a fresh session sem histórico da conversa. Invoque o planner no mínimo uma vez e no máximo três vezes for `1/3`, `2/3`, `3/3`; never a fourth call. Send only operation/slice, `SPEC_PATH`, authority/revision, relevant scope/references, compact prior evidence and recovery context. Never send installation/skill/runtime/harness paths, packaged filenames, schemas, secrets or full logs.

The runner returns only independent read-only discovery as `stnl-validation-plan/v1`; it does not execute or return `TESTS_PASS`, exits, counts or provenance. Reject result-shaped output before harness handling. The owner then invokes the packaged bridge from its loaded skill location. Bridge invocation is allowed orchestration, not direct toolchain execution. It validates, dispatches the harness, keeps the envelope out of model output and stages derived evidence in an isolated candidate. The owner publishes only an accepted candidate and confirms read-back. Não execute no contexto principal testes, builds, linters, typechecks, compilações or validators. Não faça fallback.

`TESTS_NOT_APPLICABLE` requires objective discovery and sealed proof that nenhum comando de verificação executado. Real `TESTS_FAIL` in rounds 1 or 2 permits only bounded in-slice correction; round 3 enters `IMPLEMENTATION_RETRY_EXHAUSTED` and a única próxima ação da slice é `VALIDATE_SLICE`. Never create Validation Attempt, Effective Validation Base or `[x]` here.

Initialization/transport gets no máximo uma nova tentativa técnica with identical minimal payload. Attempts não consomem rodada `N/3`, do not create `implementation-check-NN` and do not authorize correction. Malformed planner output has no transport retry. Preserve one historical Delegation Blocker.

Contexto adicional (opcional):
