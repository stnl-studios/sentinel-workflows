Use `stnl-slice-executor`.
OPERATION=EXECUTE_SLICE
SPEC_PATH={{SPEC_PATH}}
SLICE={{SLICE}}
MANAGED_WORKSPACE={{MANAGED_WORKSPACE}}
Treat the concrete recovery operation and slice returned by the shared deterministic preflight as authority; derive neither from the current request, and report both exactly when blocked.
Leia `Requirements authority` exclusivamente do campo exato `authority=sha256:<64hex>` retornado pelo mesmo official preflight para este `SPEC_PATH`, operação e slice, exija igualdade com o artifact selecionado e copie o valor byte-identical para o payload do runner e candidate record. Nunca calcule hash raw de `shared/requirements.md` ou `feature_spec.md` nem reconstrua a authority; se preflight e artifact concordarem, um valor diferente criado pelo producer não é documentary divergence.
Para o check auxiliar, a autoridade é somente o token exato do official preflight e sua igualdade com payload e artifact; nunca calcule, compare ou use SHA raw de `shared/requirements.md` ou `feature_spec.md` como authority. Um raw digest não é authority e não pode bloquear quando os três tokens oficiais concordam.
Ao classificar o retorno do runner, se preflight, payload e artifact têm o mesmo token oficial, descarte qualquer SHA raw mencionado na resposta: não o copie, não o compare, não o reporte como divergence e não crie `BLOCKED` por causa dele. Somente ausência ou desigualdade do token oficial pode bloquear a authority; schema de runner realmente malformado continua `RUNNER_RESULT_BLOCKED` por sua causa própria.
Antes da delegação, resolva uma única vez o `SPEC_PATH` canônico, o `executionRoot` derivado e os `PLAN_PATH` e `TASK_PATH` concretos dos artifacts selecionados no mesmo managed workspace; verifique por `lstat` que os quatro paths são absolutos, canônicos, existentes e pertencem ao mesmo managed workspace. Copie esses valores byte-identical para o payload do runner. Nunca envie path do source checkout, CWD, candidate, relativo, placeholder ou reconstruído; se algum path não existir, corrija a derivação antes de invocar o runner e não registre esse erro de handoff como `BLOCKED` auxiliar.
Antes de aceitar qualquer status do runner, confira cada entrada file-backed de `Tested state` pela base `dirname(tasks/slice-NN.md)` até o target físico; `src/...`, `test/...`, path absoluto, CWD-relative ou repository-relative é saída malformada. Não rebaseie nem aceite esses paths como equivalentes: exija que o runner retorne o claim task-relative canônico.
Depois de derivar cada claim, resolva `path.resolve(dirname(taskArtifact), claim)` e compare por `realpath` com o target físico que será hashado; não derive o claim de `SPEC_PATH`, SPEC root, plan, CWD ou workspace root, não omita um parent da base do task artifact e não aceite `TESTS_PASS` quando a resolução não for exatamente o target.
Antes de aceitar `TESTS_PASS` do runner, confira novamente cada digest file-backed por formato exato `sha256:` mais 64 caracteres hexadecimais minúsculos. Se qualquer token tiver comprimento ou formato diferente, trate a resposta como malformed output e mantenha `RUNNER_RESULT_BLOCKED`; não recalcule, trunque, complete ou aceite o valor.
Inclua no payload do runner `RUNNER_EVIDENCE_SERIALIZER=__RUNNER_EVIDENCE_SERIALIZER__`. O launcher fornece os valores absolutos e canônicos `MANAGED_WORKSPACE={{MANAGED_WORKSPACE}}`, `SPEC_PATH={{SPEC_PATH}}` e `SLICE={{SLICE}}`; copie-os exatamente e não use placeholders. Depois de decidir semanticamente os checks e comandos, retorne somente um objeto JSON puro, sem markdown, preâmbulo ou pós-escrito, com exatamente as chaves semânticas lowerCamelCase `status`, `automaticCheckRound`, `head`, `discoverySources`, `discoveryActions`, `verificationTypesConsidered`, `nonApplicabilityRationale`, `noVerificationCommandConfirmation`, `commands`, `resultOfEachCommandAndExitCode`, `selectedChecks`, `selectionRationale`, `coverage`, `failures`, `priorRoundFailure`, `correctionApplied`, `inSliceRationale`, `evidenceOrFailureSummary`, `affectedFilesOrBehaviors`, `blockers`, `unexpectedWorkspaceEffects` e `persistenceSummary`; em `automaticCheckRound` diferente de `1/3`, os três campos de correção devem ser objetivos e não `none`; `commands` é um array de objetos com exatamente `command` e `exit`, e não contém labels ou tuples formatados. O contexto principal persiste esse JSON sem alteração em arquivo temporário externo e invoca exatamente `node "$RUNNER_EVIDENCE_SERIALIZER" --execution-bundle --operation <requested-operation> --workspace "{{MANAGED_WORKSPACE}}" --spec-path "{{SPEC_PATH}}" --slice "{{SLICE}}" --semantic-response-file <absolute-temp-file>`. A boundary determinística rejeita chaves ausentes, desconhecidas ou valores ambíguos, canonicaliza `Changed Areas` e `Corrections Applied` usando os targets físicos aprovados em `Checklist expected areas` e `path.relative(dirname(taskArtifact), target)` antes da candidate validation, aceita alias relativo ao workspace somente quando ele identifica unicamente um target aprovado e bloqueia claims ambíguos ou irresolvíveis; depois deriva os targets canônicos, verifica `realpath`, calcula `path.relative` com `/`, deriva `Correction paths` e `Updated scope` da autoridade do task artifact, calcula o próximo identificador e emite diretamente o registro canônico `### implementation-check-NN`/`### findings-check-NN` com apenas os campos permitidos pela seção. O contexto principal copia esse registro byte-for-byte para o artifact e candidate, sem remapear labels de resposta. Se o JSON ou a serialização sair diferente de zero, retorne `Status: BLOCKED` no contexto principal; não repare depois da candidate validation nem publique saída inválida.
Every file-backed `Tested state` tuple MUST use the literal `sha256:` delimiter; `sha256=` or any other separator is malformed output and remains `RUNNER_RESULT_BLOCKED`.
Exija forma estrita do payload semântico: JSON válido, objeto único, sem markdown, exatamente as chaves lowerCamelCase da operação, todos os valores escalares como strings sem quebras de linha/backticks e `commands` como array de `{command, exit}` com `exit` inteiro. Não escreva `Operation`, `Tested scope`, `Tested state`, labels mecânicos, paths, hashes, records ou tuples formatados; esses valores pertencem ao producer determinístico. Se qualquer valor semântico não puder ser reportado exatamente, retorne um objeto JSON completo com `status: "BLOCKED"` e a causa concreta; nunca invente chaves, traduza labels, acrescente campos ou aceite/repare JSON ambíguo.
Depois de implementar, finalize o escopo no artefato detalhado: `Changed Areas` não pode permanecer `- pending` após qualquer trabalho file-backed. Inclua cada arquivo final afetado ou removido; reserve `- none` exclusivamente para uma execução genuinamente fileless e registre a razão objetiva exigida pelo contrato. Essa escolha de escopo permanece semântica do executor; o serializer somente canonicaliza e valida os claims.
Para cada campo file-backed definido pelo execution-record contract como task-relative, derive o valor persistido do target físico final com `path.relative(dirname(tasks/slice-NN.md), target)` e normalize separadores para `/`. Use o artefato detalhado final como base em `Changed Areas`, `Tested scope` e `Tested state`; o serializer determina os claims path-bearing de `Tested scope` a partir dos `--target`/`--removed` físicos e não aceita a versão calculada pelo modelo como authority. Nunca use CWD, workspace root, repository root, SPEC root ou candidate root, e não converta silenciosamente uma saída com base errada.
Antes de anexar `TESTS_PASS`, `TESTS_NOT_APPLICABLE` ou a terceira falha, persista e verifique todos os itens obrigatórios do checklist no mesmo candidate isolado. Se o preflight apontar a inconsistência conhecida de checklist incompleto após evidência terminal, retome somente `EXECUTE_SLICE` na slice indicada, complete o registro de execução sem criar novo check após o terminal existente e deixe a autoridade normal de validação retornar automaticamente.
Após implementar e registrar o escopo alterado, delegue obrigatoriamente a primeira chamada dos testes desta operação em uma sessão independente, sem histórico da conversa, com somente `OPERATION=EXECUTE_SLICE`, `SPEC_PATH`, execution root derivado, slice, paths de plans e tasks, Requirements authority, Plan revision, o official execution validator/preflight aplicável, evidências compactas relevantes, escopo alterado, rodada automática e contexto adicional estritamente necessário para:
@agent-stnl-validation-runner
Invoque o runner no mínimo uma vez e no máximo três vezes nesta mesma operação manual, nas rodadas `1/3`, `2/3` e `3/3`; nunca faça uma quarta chamada nem use loop ilimitado. Não pule o runner por mudança simples nem por acreditar que nenhum check seja aplicável; a descoberta independente e `TESTS_NOT_APPLICABLE` pertencem ao runner.
Na delegação nativa do runner, configure o schema estruturado absoluto `__RUNNER_RESPONSE_SCHEMA__` no mecanismo de saída estruturada do provedor. Esse schema é um gate de shape antes da resposta final, não um reparo: não delegue sem ele, não mapeie chaves traduzidas e não altere a resposta se o gate falhar. Os valores semânticos continuam sendo decisão do runner.
Não passe logs completos. Não execute no contexto principal testes, builds, linters, typechecks, compilações ou outros comandos de verificação. Aguarde cada retorno e persista-o append-only em `Implementation Test Evidence`, com o próximo `implementation-check-NN` global, rodada, descoberta de checks, justificativa de não aplicabilidade, estado, comandos, falhas, correções cobertas e escopo.
Em `TESTS_FAIL` nas rodadas 1 ou 2, depois de persistir a evidência, corrija automaticamente no contexto principal somente a falha objetiva dentro do escopo aprovado, registre falha, evidência, alteração, arquivos, escopo atualizado e justificativa interna, e então chame o runner novamente. Na terceira falha, persista, entre em `IMPLEMENTATION_RETRY_EXHAUSTED` e encerre sem nova correção automática; a única próxima ação da slice é `VALIDATE_SLICE`, e `EXECUTE_SLICE`/`APPLY_FINDINGS` não podem reentrar antes do veredito formal.
Aceite somente `TESTS_PASS`, `TESTS_FAIL`, `TESTS_NOT_APPLICABLE` ou `BLOCKED`. Encerre em `TESTS_PASS`, `TESTS_NOT_APPLICABLE`, `BLOCKED`, terceira falha ou decisão fora do escopo; mantenha a slice aberta e não crie Validation Attempt, Effective Validation Base ou conclusão `[x]`. `TESTS_NOT_APPLICABLE` exige descoberta objetiva, fontes e ações read-only resumidas, justificativa e nenhum comando de verificação executado; não o trate como `PASS` formal.
Se a sessão não iniciar ou o transporte falhar, faça no máximo uma nova tentativa técnica em outra sessão independente com o mesmo payload mínimo. Essas tentativas não consomem rodada `N/3`, não criam `implementation-check-NN`, não autorizam correção e, após a segunda falha, persistem o `Delegation Blocker` canônico de inicialização (compatibilidade: `Runner Initialization Blocker`); saída malformada usa o mesmo singleton com `Kind: malformed-output` e não recebe retry de transporte. Não faça fallback, não crie etapa manual de retry, não amplie escopo e não inicie `VALIDATE_SLICE` nem outra operação automaticamente.
Se a mesma operação retornar após implementação e escopo já persistidos com bloqueio anterior exclusivamente de inicialização, retome diretamente na delegação: não reimplemente, não duplique checklist, diff, evidência ou blocker, não reinicie identificadores e não crie rodada antes de o runner iniciar. Nunca crie scratch file, script auxiliar, manifest ou relatório ad hoc dentro da SPEC; reporte qualquer path não canônico exatamente e use somente diretório temporário externo quando necessário.

Para a resposta final semântica, não invoque serializer em modo `--response` nem escreva labels mecânicos. O contexto principal usa uma única chamada `node "$RUNNER_EVIDENCE_SERIALIZER" --execution-bundle --operation <requested-operation> --workspace "{{MANAGED_WORKSPACE}}" --spec-path "{{SPEC_PATH}}" --slice "{{SLICE}}" --semantic-response-file <absolute-temp-file>`; copie somente o registro canônico completo `### implementation-check-NN` ou `### findings-check-NN` emitido pela boundary determinística para o artifact e candidate. Não remapeie labels de resposta no contexto principal e não faça reparo após rejeição.

Antes dessa chamada, materialize o arquivo semântico somente pela boundary determinística `node "__RUNNER_RESPONSE_CAPTURE__" --structured-output-file <absolute-runner-output> --output <absolute-temp-file>`, usando o JSONL bruto da sessão delegada; ela extrai o último `agent_message` e grava seu objeto JSON sem alteração. Não use `JSON.stringify`, `jq -R`, quoting manual ou outro extrator, não desembrulhe string JSON e não faça reparo após rejeição.

Contexto adicional (opcional):
