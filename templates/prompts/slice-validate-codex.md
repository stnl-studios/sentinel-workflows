Use `stnl-slice-quality-manager`.
OPERATION=VALIDATE_SLICE
SPEC_PATH={{SPEC_PATH}}
SLICE={{SLICE}}
MANAGED_WORKSPACE={{MANAGED_WORKSPACE}}
CANDIDATE_EXECUTION_ROOT={{CANDIDATE_EXECUTION_ROOT}}
BENCHMARK_SEQUENCE={{BENCHMARK_SEQUENCE}}
When recording prior-slice overlap, keep the semantic rationale in the compact form `- Slice NN overlap: <path claims>; <behavior and regression rationale>` (or the existing canonical `- Paths: ...` form). The deterministic producer consumes only the physical path claims and owns canonical labels, ordering, task-relative serialization, and identity checks; do not hand-repair its output after rejection.
Treat the concrete recovery operation and slice returned by the shared deterministic preflight as authority; derive neither from the current request, and report both exactly when blocked.
Leia `Requirements authority` exclusivamente do campo exato `authority=sha256:<64hex>` retornado pelo mesmo official preflight para este `SPEC_PATH`, operação e slice, exija igualdade com o artifact selecionado e copie o valor byte-identical para o payload do runner e candidate record. Nunca calcule hash raw de `shared/requirements.md` ou `feature_spec.md` nem reconstrua a authority; se preflight e artifact concordarem, um valor diferente criado pelo producer não é documentary divergence.
Em `VALIDATE_SLICE`, o campo semântico `commands` contém somente verification commands escolhidos pelo runner; não inclua o official execution validator/preflight nem invente paths, labels ou placeholders mecânicos. O producer determinístico launcher-owned insere como primeiro command o official execution validator/preflight completo, absoluto, com o `SPEC_PATH` exato, operação, slice numérico e exit code `0` conhecido pelo preflight já executado. Se uma entrada semântica tentar emitir esse comando launcher-owned, a boundary deve bloquear por output malformado, sem corrigir ou reapresentar.
Antes de aceitar qualquer `PASS`, confira todos os digests file-backed de `Tested state` e do manifesto formal: cada token deve ser exatamente `sha256:` mais 64 caracteres hexadecimais minúsculos obtidos por comando read-only. Não digite, trunque, arredonde, recalcule ou repare um digest; token com comprimento ou formato diferente é saída malformada e deve permanecer `BLOCKED`.
Inclua no payload do runner `RUNNER_EVIDENCE_SERIALIZER=__RUNNER_EVIDENCE_SERIALIZER__`. Copie exatamente `MANAGED_WORKSPACE`, `SPEC_PATH`, `SLICE` e `CANDIDATE_EXECUTION_ROOT` fornecidos acima; não use placeholders. O runner decide semanticamente o veredito, findings, evidência e verification commands e retorna somente um objeto JSON de `VALIDATE_SLICE` com as chaves lowerCamelCase exatas `status`, `head`, `commands`, `evidence`, `findingReferences`, `findingDispositions`, `blockers`, `unexpectedWorkspaceEffects` e `persistenceSummary`; não inclua labels, paths relativos, records, manifest, `verifiedScope`, `type` ou `operation`. `commands` é array de objetos `{command, exit}`. Sem findings prévios, use o par `none`/`none`, exceto quando `NEEDS_FIX` introduzir findings novos na edição semântica do candidate; não use sinônimos como `unchanged`. Delimitadores Markdown balanceados são removidos mecanicamente; delimitadores desbalanceados bloqueiam.

O driver oficial já preparou `CANDIDATE_EXECUTION_ROOT` como cópia completa e byte-identical da execution tree live, isolada fora da SPEC e da execution root, antes deste turno. Não copie, recrie ou mova essa árvore. Após o runner retornar, faça somente as edições semânticas necessárias em `Validation Findings` e `Diff Summary`, e mantenha `Validation Attempts`, `Effective Validation Base`, `Final Result` e a row selecionada de `tasks.md` byte-identical ao live. Grave o semantic response unchanged em arquivo temporário fora da SPEC. Invoque uma única vez `node "__VALIDATION_CANDIDATE_PREPARER__" --prepare --spec-path "{{SPEC_PATH}}" --slice "{{SLICE}}" --workspace "{{MANAGED_WORKSPACE}}" --candidate-execution-root "{{CANDIDATE_EXECUTION_ROOT}}" --semantic-response-file <absolute-semanticResponseFile>`. Essa boundary reutiliza execution-state e runner-evidence serializer para derivar attempt ID/type, scope, targets físicos, official command, paths task-relative e SHA-256 completos; escreve o attempt e, somente em `PASS`, base, final e tasks row. Para `NEEDS_FIX` ou `BLOCKED`, grava somente o attempt. Em seguida rode o validator oficial estrito do candidate. Se a preparação falhar, bloqueie antes da validação; se a validação rejeitar, preserve a rejeição e não reapresente nem repare o candidate.
Se evidência terminal de implementação coexistir com checklist obrigatório incompleto, não inicie validação: preserve e reporte exatamente o recovery target `stnl-slice-executor / EXECUTE_SLICE / slice-NN` retornado pelo preflight. Validação volta a ser legal somente após essa mesma slice ficar completa.

O launcher preserva o JSONL bruto e executa internamente a mesma boundary determinística `captureRunnerResponse` antes de retornar o `semanticResponseFile`; não extraia, reserializa, traduza ou repare a mensagem final.
Antes do spawn, resolva uma única vez o valor concreto de `SPEC_PATH` deste launcher, exija que ele seja absoluto, canônico, existente e aceito pelo official preflight, e copie esse valor byte-identical para o payload do runner. Nunca o substitua por execution root, task path, candidate root, path relativo ao CWD, placeholder ou path reconstruído. Use somente o nome registrado exato indicado pela chamada abaixo; não tente iniciar o runner por shell ou tratando um path como agent identifier.
Faça a validação independente pelo runner configurado: envie o payload semântico mínimo por stdin ao único launcher permitido `node "__RUNNER_INVOCATION_HELPER__" --operation VALIDATE_SLICE --sequence {{BENCHMARK_SEQUENCE}} --slice "{{SLICE}}" <<'SENTINEL_RUNNER_REQUEST'`. Inclua somente `SPEC_PATH`, execution root derivado, slice, paths de plans e tasks, Requirements authority, Plan revision, official execution validator/preflight aplicável, evidências compactas de implementação e findings, incluindo `TESTS_NOT_APPLICABLE`, resumo de findings ativos/históricos e tentativas válidas, escopo alterado, diff, overlaps e contexto estritamente necessário; não envie histórico da conversa. O helper lê as instruções e perfil exatos de `.codex/agents/stnl_validation_runner.toml`, inicia uma nova sessão `codex exec` sem histórico, anexa ao argv real o schema fechado de validação, desabilita delegação multi-agent, persiste o JSONL bruto e chama `captureRunnerResponse` sem reescrita. Feche o heredoc com `SENTINEL_RUNNER_REQUEST`. O recibo `SENTINEL_RUNNER_RECEIPT` retorna o `semanticResponseFile`; passe-o byte-for-byte ao producer de bundle. Invoque o runner no mínimo uma vez e no máximo três vezes nesta sequência oficial, nas rodadas `1/3`, `2/3` e `3/3`; nunca faça uma quarta chamada. Cada invocação conta como tentativa e não há fallback, subagent nativo ou comando `codex exec` montado pelo modelo.
Determine pelo global plan e pela ordem serial se esta é a última slice aberta da revisão corrente. Somente nesse caso inclua no payload o global plan, a ordem serial, summaries e final results anteriores, Effective Validation Bases pertinentes, current requirements authority, coverage/reconciliation e riscos de overlap ou integração necessários. Instrua o runner a avaliar cobertura global, reconciliação com a authority corrente, consistência cross-slice e integration/stabilization work necessário ainda não materializado. Falta de authority, estratégia ou integração necessária retorna `BLOCKED` com causa concreta e handoff para `REPLAN`; não produza PASS terminal artificial, segunda Validation Attempt, nova operação ou slice automática.
Não passe logs completos. Aguarde o retorno. O contexto principal adiciona a Validation Attempt, persiste as disposições completas de findings existentes e novos findings estruturados e, em `PASS` válido, exige zero finding bloqueante ativo, substitui a Effective Validation Base e finaliza a slice; não repete testes, não refaz a validação e não emite outro veredito.
Exija revisão independente da descoberta e justificativa de qualquer `TESTS_NOT_APPLICABLE`; o runner pode rejeitar essa evidência, descobrir e executar check aplicável ou exigir inspeção adicional. Não promova não aplicabilidade a `PASS`; a validação formal continua somente `PASS | NEEDS_FIX | BLOCKED`.
Se a sessão não iniciar ou o transporte falhar, faça no máximo uma nova tentativa técnica em outra sessão independente com o mesmo payload mínimo; essas tentativas não consomem rodada, não criam nem consomem `attempt-NN`, não mudam `initial` para `revalidation` e, após a segunda falha, mantêm somente o `Delegation Blocker` canônico de inicialização (compatibilidade: `Runner Initialization Blocker`). Saída malformada usa o mesmo singleton com `Kind: malformed-output`, não recebe retry de transporte e não cria tentativa fabricada. Em nova chamada bloqueada exclusivamente pela inicialização, retome diretamente no spawn sem duplicar blocker ou reiniciar o próximo identificador; a recuperação de saída malformada segue o mesmo gate. Não faça fallback nem substitua, suavize ou promova o resultado.
Nunca crie scratch file, script auxiliar, manifest ou relatório ad hoc dentro da SPEC; reporte qualquer path não canônico exatamente, não o apague e use somente diretório temporário externo quando necessário.

Para `VALIDATE_SLICE`, o runner não invoca `--record`, `--manifest`, `--response` ou `--validation-bundle`; somente o contexto principal chama uma vez o candidate preparer antes do validator. Nunca escreva manualmente fields mecânicos, nem chame o preparer depois de uma rejeição.

Contexto adicional (opcional):
