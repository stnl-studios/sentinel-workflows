# File Purpose Header

```yaml
purpose: Keep SPEC work compact through selective reading and non-duplicated records.
status: not_applicable
read_when: A lifecycle operation risks broad reads, repeated prose, or unnecessary artifacts.
do_not_read_when: A tiny isolated correction has all needed context.
contains: Reading rules, compactness limits, and closure compaction rules.
owner: stnl-spec-lifecycle-manager
update_policy: Change when the documentary economy policy changes.
```

# Token Economy

Prefer canonical IDs to repeated prose. Treat `feature_spec.md` as the documentary guide and `shared/` as the location of materialized canonical details.

Read the smallest useful set for the current MODE. Do not load every shared category when the relevant ID or concern identifies a narrower file. Do not store command output, chats, session summaries, internal reasoning, permanent handoffs, repeated criteria, or duplicate traceability.

CLOSE may inspect all materialized records needed for consistency, but copies only durable requirements content into the final file.
