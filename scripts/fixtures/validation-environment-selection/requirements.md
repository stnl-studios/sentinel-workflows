# Multi-repository validation environment SPEC

- AC-001: validate the API source with the API profile selected from project configuration.
- AC-002: validate the web source with a concrete project profile; ask when more than one supported profile remains applicable.
- The slice covers `components/service-api/src/message.txt` and `components/web-client/src/message.txt`.

