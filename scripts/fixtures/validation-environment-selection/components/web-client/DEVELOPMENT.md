# Web client development

Both aggregator profiles tagged for `web-client` are supported. Compose is recommended because it matches the shared project toolchain; host remains explicitly authorized when the operator selects it.

